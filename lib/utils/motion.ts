// Re-export from the canonical MD3 motion module for backward compatibility
export * from "@/lib/theme/motion";

// Legacy aliases still used by older components
export { buttonPress, hoverLift, itemEntry, staggerContainer, fadeThrough as pageTransition, dialogAnim as modalEntry } from "@/lib/theme/motion";
